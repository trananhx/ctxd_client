# UI Reskin (ghép art GCLD) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Skin lại 7 prefab UI (5 màn + HUD + TEST panel) bằng art thật trong `Assets/Resources/sprite/` + placeholder composite màu; thêm avatar/sao nộ/buff/skill-name-pop cho HUD trận.

**Architecture:** 3 lớp — (1) `SkinIndexForge` (Editor) sinh `CtxdSpriteIndex.asset` giải hash tên file cho runtime-lookup + helper `FindSprite` cho bake-time; (2) `CtxdSkin.cs` runtime: palette + `CtxdArt` (tra index, cache) + bảng map id→art; (3) `UIForge` viết lại 8 helper (composite viền, ColorBlock) + mở rộng builder/UI class từng màn. Server/protocol KHÔNG đổi; Director chỉ +2 dòng gọi API HUD mới.

**Tech Stack:** Unity 6000.2.8f1, uGUI + TMP, LuzartUI (UIBase<TData>), DOTween (Demigiant, có sẵn), Unity MCP để rebuild/verify.

## Global Constraints

- **KHÔNG sinh file ảnh mới** (chỉ đạo chủ dự án); thiếu art → composite Image màu lồng nhau.
- **KHÔNG hardcode hash tên file** — bake-time dùng `SkinIndexForge.FindSprite`, runtime dùng `CtxdSpriteIndex`.
- **KHÔNG sửa protocol/server data/chiến trường** (UnitVisual/BattleSideField/303 FX prefab).
- Sprite kho import `spriteMode: 2` → mọi load runtime dùng `Resources.LoadAll<Sprite>(path)` lấy `[0]`.
- Layout tổng thể (anchor/vị trí các cụm) GIỮ NGUYÊN trừ chỗ plan nói rõ — flow test cũ bấm nút theo tên GameObject (= label nút) phải tiếp tục chạy: **KHÔNG đổi tên GameObject nút**.
- Font giữ Saira/TMP default; mọi text mới qua helper `StyleText` hiện có.
- Sau MỖI task: `refresh_unity(compile)` + `read_console` 0 error mới, rồi commit (`feat(ui-skin): …`).
- Unity MCP: active instance `ctxd_client@f062b543`; rebuild = menu `CTXD/Server/Build Server Battle Scene (UI + wiring)`.
- Verify Đợt 2 chạy thêm `dotnet build ctxd.sln` *(nếu sln ở root — nếu không: `dotnet test Server.Tests`)* kỳ vọng như baseline 114/114.

## Bảng art then chốt (đã xác minh bằng mắt)

| Key index | Nguồn | Ghi chú |
|---|---|---|
| `tacticalGeneralPicMax/<rosterId>` | pinyin trùng id roster | fallback: ẩn Image |
| `warBG/1` `warBG/5` `warBG/11` `warBG/100` … (JPG) | phong cảnh | map stage bảng dưới |
| `warBuff/<n>` | icon buff tròn | default `warBuff/1` |
| `warFeatAnger/featAnger<1..6>` | sao nộ 6 cấp | |
| `warSkillName/<n>` | thư pháp TIẾNG VIỆT tên chiêu | map từ bảng OCR (đang chạy agent) |
| `windowBG/1` | cuộn giấy 1024×600 | Result |
| `eff/Recruit/1` | tranh kỵ binh ngang | Lobby hero (bake-time, không cần index) |

Map stage→bg: `stage_khanhvang→warBG/1`, `stage_fxdemo→warBG/1`, `stage_quando→warBG/11`, `stage_bachho→warBG/5`, `stage_xichbich→` *(Task 6 xem 3-4 JPG chưa xem — 2,3,4,7 — chọn ảnh có nước; không có → `warBG/100`)*. Fallback Terrain: Plain→1, Mountain→5, Pass/City→11, Water→(như xichbich), khác→1.

---

### Task 1: SkinIndexForge + CtxdSpriteIndex (Editor sinh index giải hash)

**Files:**
- Create: `Assets/Ctxd/UI/CtxdSpriteIndex.cs` (runtime SO — để asset load được ở player)
- Create: `Assets/Ctxd/Editor/SkinIndexForge.cs`
- Output asset: `Assets/Ctxd/Resources/CtxdSpriteIndex.asset` (cạnh `CtxdGameDatabase.asset` — pattern Resources.Load có sẵn)

**Interfaces (Produces):**
- `CtxdSpriteIndex : ScriptableObject` — `bool TryGet(string key, out string resPath)`; key dạng `"warBuff/10"`, resPath dạng `"sprite/warBuff/10_467f1de1f02e8638"` (path Resources không đuôi).
- `SkinIndexForge.Build() : CtxdSpriteIndex` + `[MenuItem("CTXD/Forge/Build Sprite Index")]`.
- `SkinIndexForge.FindSprite(string folderUnderSprite, string prefix) : Sprite` — bake-time cho UIForge.

- [ ] **Step 1: Viết `CtxdSpriteIndex.cs`**

```csharp
using System.Collections.Generic;
using UnityEngine;

namespace Ctxd.UI
{
    /// <summary>
    /// Chỉ mục "tên logic → đường dẫn Resources" cho kho sprite rip (tên file có hash md5 —
    /// KHÔNG hardcode hash trong code/prefab; forge sinh asset này, runtime tra rồi LoadAll).
    /// </summary>
    public sealed class CtxdSpriteIndex : ScriptableObject
    {
        [System.Serializable]
        public struct Entry { public string Key; public string ResPath; }

        [SerializeField] private List<Entry> entries = new List<Entry>();

        private Dictionary<string, string> _lookup;

        public void SetEntries(List<Entry> e) { entries = e; _lookup = null; }
        public int Count => entries?.Count ?? 0;

        public bool TryGet(string key, out string resPath)
        {
            if (_lookup == null)
            {
                _lookup = new Dictionary<string, string>(entries.Count);
                foreach (var en in entries) if (!_lookup.ContainsKey(en.Key)) _lookup.Add(en.Key, en.ResPath);
            }
            return _lookup.TryGetValue(key, out resPath);
        }
    }
}
```

- [ ] **Step 2: Viết `SkinIndexForge.cs`**

Quy tắc key: tên file bỏ đuôi; nếu kết thúc bằng `_<16 ký tự hex>` thì cắt phần đó. Quét đệ quy các thư mục: `tacticalGeneralPicMax`, `warBuff`, `warSkillName`, `warFeatAnger`, `warState`, `warvsicon`, `windowBG`, và riêng `warBG` chỉ lấy file NGAY TRONG thư mục (bỏ `warBG/images` — bitmap số). Key = `<thưMụcGốc>/<tênCắtHash>`; trùng key → giữ entry đầu (sort trước cho ổn định).

```csharp
using System.Collections.Generic;
using System.IO;
using System.Linq;
using System.Text.RegularExpressions;
using UnityEditor;
using UnityEngine;
using Ctxd.UI;

namespace Ctxd.EditorTools
{
    /// <summary>Sinh CtxdSpriteIndex.asset (tên logic → path Resources) + tra sprite bake-time cho UIForge.</summary>
    public static class SkinIndexForge
    {
        const string SpriteRoot = "Assets/Resources/sprite";
        const string AssetPath = "Assets/Ctxd/Resources/CtxdSpriteIndex.asset";
        static readonly Regex Hash = new Regex("_[0-9a-f]{16}$");
        // warBG: chỉ file phẳng (images/ là bitmap font số). Các mục khác quét đệ quy.
        static readonly (string folder, bool recurse)[] Folders =
        {
            ("tacticalGeneralPicMax", true), ("warBuff", true), ("warSkillName", true),
            ("warFeatAnger", true), ("warState", true), ("warvsicon", true),
            ("windowBG", true), ("warBG", false),
        };

        [MenuItem("CTXD/Forge/Build Sprite Index")]
        public static void BuildMenu() { var so = Build(); Debug.Log($"[SkinIndexForge] {so.Count} entries → {AssetPath}"); }

        public static CtxdSpriteIndex Build()
        {
            var entries = new List<CtxdSpriteIndex.Entry>();
            foreach (var (folder, recurse) in Folders)
            {
                string abs = Path.Combine(SpriteRoot, folder).Replace('\\', '/');
                if (!AssetDatabase.IsValidFolder(abs)) { Debug.LogWarning($"[SkinIndexForge] thiếu {abs}"); continue; }
                var files = Directory.EnumerateFiles(abs, "*.*", recurse ? SearchOption.AllDirectories : SearchOption.TopDirectoryOnly)
                    .Where(p => p.EndsWith(".png") || p.EndsWith(".jpg")).OrderBy(p => p);
                foreach (var f in files)
                {
                    string noExt = Path.ChangeExtension(f, null).Replace('\\', '/');
                    string key = folder + "/" + Hash.Replace(Path.GetFileName(noExt), "");
                    string resPath = noExt.Substring(noExt.IndexOf("Resources/") + "Resources/".Length);
                    entries.Add(new CtxdSpriteIndex.Entry { Key = key, ResPath = resPath });
                }
            }
            var so = AssetDatabase.LoadAssetAtPath<CtxdSpriteIndex>(AssetPath);
            if (so == null) { so = ScriptableObject.CreateInstance<CtxdSpriteIndex>(); AssetDatabase.CreateAsset(so, AssetPath); }
            so.SetEntries(entries);
            EditorUtility.SetDirty(so); AssetDatabase.SaveAssets();
            return so;
        }

        /// <summary>Bake-time: tìm Sprite theo prefix tên file trong 1 thư mục dưới sprite/ (vd ("eff/Recruit","1")).</summary>
        public static Sprite FindSprite(string folderUnderSprite, string prefix)
        {
            string dir = SpriteRoot + "/" + folderUnderSprite;
            foreach (var guid in AssetDatabase.FindAssets("t:Sprite", new[] { dir }))
            {
                string p = AssetDatabase.GUIDToAssetPath(guid);
                string name = Path.GetFileNameWithoutExtension(p);
                if (name == prefix || name.StartsWith(prefix + "_"))
                    return AssetDatabase.LoadAssetAtPath<Sprite>(p);
            }
            Debug.LogWarning($"[SkinIndexForge] không thấy sprite {folderUnderSprite}/{prefix}");
            return null;
        }
    }
}
```

- [ ] **Step 3: Compile + sinh index** — MCP `refresh_unity(compile=request)`; `read_console` 0 error; `execute_menu_item("CTXD/Forge/Build Sprite Index")`; console phải log `[SkinIndexForge] N entries` với N ≈ 500-650 (358 chân dung + 88 buff + 145 skillname + ~46 khác).
- [ ] **Step 4: Spot-check index** — MCP `execute_code`: load asset, in `TryGet("tacticalGeneralPicMax/guanyu")`, `TryGet("warBG/1")`, `TryGet("warFeatAnger/featAnger6")` → cả 3 true; `Resources.LoadAll<Sprite>(path)` với path guanyu trả ≥1 sprite.
- [ ] **Step 5: Commit** — `git add Assets/Ctxd/UI/CtxdSpriteIndex.cs* Assets/Ctxd/Editor/SkinIndexForge.cs* Assets/Ctxd/Resources/CtxdSpriteIndex.asset*` → `feat(ui-skin): sprite index giải hash + forge`.

### Task 2: CtxdSkin runtime (palette + CtxdArt + bảng map)

**Files:**
- Create: `Assets/Ctxd/UI/CtxdSkin.cs`

**Interfaces:**
- Consumes: `CtxdSpriteIndex` (Task 1).
- Produces: `CtxdPalette` (static Color); `CtxdArt`: `Sprite Load(string key)`, `Sprite Portrait(string generalId)`, `Sprite StageBg(string stageId, string terrain)`, `Sprite BuffIcon(string fxId)`, `Sprite AngerStar(int level1to6)`, `Sprite SkillNamePic(string skillId)`, `string FormatTroops(int cur, int max)`.

- [ ] **Step 1: Viết `CtxdSkin.cs`**

```csharp
using System.Collections.Generic;
using UnityEngine;

namespace Ctxd.UI
{
    /// <summary>Palette cổ trang dùng chung cho mọi màn (placeholder composite — không texture).</summary>
    public static class CtxdPalette
    {
        public static readonly Color BgDark      = Hex("#141210", 0.95f);
        public static readonly Color PanelFill   = Hex("#1E1A14", 0.96f);
        public static readonly Color PanelBorder = Hex("#8A6B32");
        public static readonly Color BtnBorder   = Hex("#C9A34D");
        public static readonly Color BtnCrimson  = Hex("#7A2E22");
        public static readonly Color BtnBlue     = Hex("#2E4A66");
        public static readonly Color BtnGold     = Hex("#8A6B1F");
        public static readonly Color TxtTitle    = Hex("#FFD25E");
        public static readonly Color TxtBody     = Hex("#D8CBB0");
        public static readonly Color TxtButton   = Hex("#FFE8B0");
        public static readonly Color BarBorder   = Hex("#5A4426");
        public static readonly Color BarSlot     = Hex("#120D0A");
        public static readonly Color HpAlly      = Hex("#37B34A");
        public static readonly Color HpEnemy     = Hex("#C8342B");
        public static readonly Color Morale      = Hex("#E8A020");
        public static readonly Color InkOnPaper  = Hex("#4A2E14");   // chữ trên cuộn giấy Result
        public static readonly Color SkillPop    = Hex("#FF9AD0");   // text thư pháp fallback

        public static Color Hex(string hex, float a = 1f)
        { ColorUtility.TryParseHtmlString(hex, out var c); c.a = a; return c; }
    }

    /// <summary>Tra art rip theo tên logic (qua CtxdSpriteIndex) + các bảng map data→art. Cache sprite đã load.</summary>
    public static class CtxdArt
    {
        static CtxdSpriteIndex _index;
        static readonly Dictionary<string, Sprite> _cache = new Dictionary<string, Sprite>();

        static CtxdSpriteIndex Index => _index != null ? _index : (_index = Resources.Load<CtxdSpriteIndex>("CtxdSpriteIndex"));

        public static Sprite Load(string key)
        {
            if (string.IsNullOrEmpty(key)) return null;
            if (_cache.TryGetValue(key, out var s)) return s;
            Sprite result = null;
            if (Index != null && Index.TryGet(key, out var resPath))
            {
                var all = Resources.LoadAll<Sprite>(resPath);        // spriteMode 2 → LoadAll, lấy [0]
                if (all != null && all.Length > 0) result = all[0];
            }
            _cache[key] = result;                                     // cache cả miss — tránh LoadAll lặp
            return result;
        }

        public static Sprite Portrait(string generalId) => Load($"tacticalGeneralPicMax/{generalId}");
        public static Sprite AngerStar(int level1to6) => Load($"warFeatAnger/featAnger{Mathf.Clamp(level1to6, 1, 6)}");

        static readonly Dictionary<string, string> StageBgMap = new Dictionary<string, string>
        {
            ["stage_khanhvang"] = "warBG/1", ["stage_fxdemo"] = "warBG/1",
            ["stage_quando"] = "warBG/11", ["stage_bachho"] = "warBG/5",
            // stage_xichbich: điền ở Task 6 sau khi chọn ảnh nước.
        };
        public static Sprite StageBg(string stageId, string terrain)
        {
            if (stageId != null && StageBgMap.TryGetValue(stageId, out var k)) return Load(k);
            string fb = terrain switch
            {
                "Mountain" => "warBG/5",
                "Pass" or "City" => "warBG/11",
                _ => "warBG/1",
            };
            return Load(fb);
        }

        static readonly Dictionary<string, string> FxToBuff = new Dictionary<string, string>();  // điền Task 5 (xem icon thật)
        public static Sprite BuffIcon(string fxId)
        {
            if (fxId != null && FxToBuff.TryGetValue(fxId, out var k)) { var s = Load(k); if (s != null) return s; }
            return Load("warBuff/1");
        }

        static readonly Dictionary<string, string> SkillNameMap = new Dictionary<string, string>();  // điền Task 5 từ bảng OCR
        public static Sprite SkillNamePic(string skillId)
            => (skillId != null && SkillNameMap.TryGetValue(skillId, out var k)) ? Load(k) : null;

        public static string FormatTroops(int cur, int max)
        {
            static string K(int v) => v >= 10000 ? $"{v / 1000f:0.#}k" : v.ToString("n0");
            return $"{K(cur)} / {K(max)}";
        }
    }
}
```

- [ ] **Step 2: Compile check** — `refresh_unity(compile=request)` + `read_console` 0 error.
- [ ] **Step 3: Smoke qua MCP `execute_code`**: `Ctxd.UI.CtxdArt.Portrait("guanyu") != null`, `CtxdArt.StageBg("stage_quando", null) != null`, `CtxdArt.AngerStar(9)` không throw (clamp) → return chuỗi kết quả 3 phép thử.
- [ ] **Step 4: Commit** — `feat(ui-skin): palette + CtxdArt (tra art rip, cache, map stage/buff/skill khung)`.

### Task 3 (Đợt 1): Reskin 8 helper UIForge → đổi da cả 7 prefab

**Files:**
- Modify: `Assets/Ctxd/Editor/UIForge.cs` (helpers dòng 355-460 + tham số màu trong 7 builder)

**Interfaces (Produces — builder nội bộ):**
- `Frame(name, parent, aMin, aMax, pivot, pos, size, Color? border = null, Color? fill = null) : RectTransform` — Image cha màu viền + con "Fill" inset 2px màu nền; trả CHA (content add vào cha, Fill vẽ dưới do add trước).
- `Panel(...)` giữ chữ ký cũ nhưng gọi `Frame` (tham số Color c → fill; viền mặc định `PanelBorder`).
- `Button(parent, label, anchor, pos, size, Color bg, float fontSize = 20)` — root Image = `BtnBorder`; con "Bg" inset 2 nhận `ColorBlock` (normal=bg, highlighted=bg×1.18, pressed=bg×0.8, disabled=bg nhạt α0.45); `Button.targetGraphic` = Bg. **Tên root GO = label (giữ nguyên — flow test tìm theo tên)**.
- `Bar(name, parent, anchor, pos, size, Color fill)` giữ chữ ký, cấu trúc mới: viền (`BarBorder`) → "Slot" inset 1 (`BarSlot`) → "Fill" inset 1 (Filled/Horizontal) — vẫn trả Image Fill.
- `BarLabeled(name, parent, anchor, pos, size, Color fill, out TMP_Text label)` — như Bar + TMP label căn giữa đè trên (fontSize = size.y×0.62, màu trắng).

- [ ] **Step 1: Viết lại helpers** (thay `Panel/Button/Bar/FullBg/Highlight`, thêm `Frame/BarLabeled`; `FullBg` → màu `CtxdPalette.BgDark`; `Highlight` viền `#FFD25E` α0.28 + không đổi hành vi). `using Ctxd.UI;` đã có sẵn. Code mẫu phần lõi:

```csharp
static RectTransform Frame(string name, Transform parent, Vector2 aMin, Vector2 aMax, Vector2 pivot,
    Vector2 pos, Vector2 size, Color? border = null, Color? fill = null)
{
    var rt = NewRect(name, parent, aMin, aMax, pivot, pos, size);
    var b = rt.gameObject.AddComponent<Image>(); b.color = border ?? CtxdPalette.PanelBorder; b.raycastTarget = false;
    var fillRt = NewRect("Fill", rt, Vector2.zero, Vector2.one, new Vector2(0.5f, 0.5f), Vector2.zero, Vector2.zero);
    fillRt.offsetMin = new Vector2(2, 2); fillRt.offsetMax = new Vector2(-2, -2);
    var f = fillRt.gameObject.AddComponent<Image>(); f.color = fill ?? CtxdPalette.PanelFill; f.raycastTarget = false;
    return rt;
}

static Button Button(Transform parent, string label, Vector2 anchor, Vector2 pos, Vector2 size, Color bg, float fontSize = 20)
{
    var rt = NewRect(label, parent, anchor, anchor, new Vector2(0.5f, 0.5f), pos, size);
    var borderImg = rt.gameObject.AddComponent<Image>(); borderImg.color = CtxdPalette.BtnBorder;
    var bgRt = NewRect("Bg", rt, Vector2.zero, Vector2.one, new Vector2(0.5f, 0.5f), Vector2.zero, Vector2.zero);
    bgRt.offsetMin = new Vector2(2, 2); bgRt.offsetMax = new Vector2(-2, -2);
    var bgImg = bgRt.gameObject.AddComponent<Image>(); bgImg.color = Color.white;
    var btn = rt.gameObject.AddComponent<Button>();
    btn.targetGraphic = bgImg;
    var cb = btn.colors;
    cb.normalColor = bg; cb.highlightedColor = bg * 1.18f; cb.pressedColor = bg * 0.8f;
    cb.selectedColor = bg; cb.disabledColor = new Color(bg.r, bg.g, bg.b, 0.45f); cb.colorMultiplier = 1f;
    btn.colors = cb;
    var t = Text(label + "_L", rt, new Vector2(0.5f, 0.5f), Vector2.zero, size, fontSize, label, CtxdPalette.TxtButton);
    var trt = (RectTransform)t.transform; trt.anchorMin = Vector2.zero; trt.anchorMax = Vector2.one; trt.offsetMin = Vector2.zero; trt.offsetMax = Vector2.zero;
    return btn;
}
```

  Trong 7 builder: đổi các Color literal cũ → palette (Lobby nền `FullBg` mới; nút XUẤT CHINH/VÀO TRẬN → `BtnCrimson`; QUAY LẠI/ĐỘI HÌNH/stub → `BtnBlue`; TIẾP TỤC/GIÁC/xác nhận → `BtnGold`; title → `TxtTitle`; card/slot nền qua `Frame`; TestPanel bg → `Frame` với fill đen α0.6). HUD `Bar` off → `HpAlly`, def → `HpEnemy`, morale 2 bên → `Morale`. KHÔNG đổi toạ độ/size/tên GO ở task này.
- [ ] **Step 2: Compile + rebuild** — `refresh_unity(compile=request)` → 0 error → `execute_menu_item("CTXD/Server/Build Server Battle Scene (UI + wiring)")` → console log `[UIForge] Built …`.
- [ ] **Step 3: Screenshot đối chiếu** — MCP play → chờ Lobby → `ScreenCapture.CaptureScreenshot` → Read PNG: nút phải có viền vàng đồng + nền màn nâu đen; bấm `XUẤT CHINH` qua `GameObject.Find("XUẤT CHINH")…onClick.Invoke()` → SelectGeneral cũng skin mới → stop play.
- [ ] **Step 4: Commit** — `feat(ui-skin): đợt 1 — palette + composite viền cho toàn bộ helper, 7 prefab đổi da`.

### Task 4 (Đợt 2a): HUD trận — avatar, sao nộ, số quân, buff row, banner ribbon, skill pop

**Files:**
- Modify: `Assets/Ctxd/UI/BattleHudUI.cs` (thêm fields + API)
- Modify: `Assets/Ctxd/Editor/UIForge.cs` — `BuildHud()`

**Interfaces:**
- Consumes: `CtxdArt`, `Frame/BarLabeled` (Task 2, 3); `CombatantSnapshot.DefId/FiveStar/Troops/MaxTroops/Morale/MoraleFull/CanCast`; `SideSnapshot.Effects` (`ActiveEffectSnapshot.FxId/RemainingRounds`).
- Produces (Director gọi ở Task 5): `void SyncSideEffects(SideSnapshot off, SideSnapshot def)`; `void ShowSkillName(string skillId, string skillName)`; `SetActiveGenerals` giữ chữ ký.

- [ ] **Step 1: BattleHudUI thêm serialized fields**

```csharp
[Header("Reskin — avatar/sao nộ/buff")]
[SerializeField] private Image _offPortrait, _defPortrait;
[SerializeField] private TMP_Text _offTroops, _defTroops;
[SerializeField] private Image[] _offStars = new Image[6];
[SerializeField] private Image[] _defStars = new Image[6];
[SerializeField] private RectTransform _offBuffRow, _defBuffRow;
[SerializeField] private GameObject _buffTemplate;          // con inactive của _offBuffRow
[Header("Reskin — skill pop")]
[SerializeField] private CanvasGroup _skillPopGroup;
[SerializeField] private Image _skillPopImage;
[SerializeField] private TMP_Text _skillPopText;
private string _offPortraitId, _defPortraitId;              // tránh Load lại mỗi snapshot
private Tween _skillPopTween;                                // using DG.Tweening;
```

- [ ] **Step 2: Mở rộng `SetActiveGenerals`** — sau phần hiện có, mỗi phe:

```csharp
if (_offPortrait != null && off.DefId != _offPortraitId)
{
    _offPortraitId = off.DefId;
    var sp = CtxdArt.Portrait(off.DefId);
    _offPortrait.sprite = sp; _offPortrait.enabled = sp != null; _offPortrait.preserveAspect = true;
}
if (_offName) _offName.text = off.FiveStar ? $"★ {off.DisplayName}" : off.DisplayName;
if (_offTroops) _offTroops.text = CtxdArt.FormatTroops(off.Troops, off.MaxTroops);
SetStars(_offStars, off.Morale, off.MoraleFull);
```

  với `SetStars`:

```csharp
private static void SetStars(Image[] stars, int morale, int full)
{
    if (stars == null) return;
    float ratio = full > 0 ? Mathf.Clamp01(morale / (float)full) : 0f;
    int lit = Mathf.FloorToInt(ratio * stars.Length + 0.0001f);
    for (int i = 0; i < stars.Length; i++)
    {
        if (stars[i] == null) continue;
        bool on = i < lit;
        stars[i].sprite = CtxdArt.AngerStar(i + 1);
        stars[i].color = on ? Color.white : new Color(1f, 1f, 1f, 0.28f);
    }
}
```

- [ ] **Step 3: `SyncSideEffects` (rebuild row ≤ 5 icon — YAGNI diff)**

```csharp
public void SyncSideEffects(SideSnapshot off, SideSnapshot def)
{
    FillBuffRow(_offBuffRow, off?.Effects);
    FillBuffRow(_defBuffRow, def?.Effects);
}

private void FillBuffRow(RectTransform row, List<ActiveEffectSnapshot> fx)
{
    if (row == null || _buffTemplate == null) return;
    for (int i = row.childCount - 1; i >= 0; i--)
    {
        var c = row.GetChild(i).gameObject;
        if (c != _buffTemplate) Destroy(c);
    }
    if (fx == null) return;
    foreach (var e in fx)
    {
        var go = Instantiate(_buffTemplate, row);
        go.SetActive(true);
        var img = go.GetComponent<Image>();
        if (img != null) img.sprite = CtxdArt.BuffIcon(e.FxId);
        var badge = go.GetComponentInChildren<TMP_Text>(true);
        if (badge != null) badge.text = e.RemainingRounds >= 0 ? e.RemainingRounds.ToString() : "";
    }
}
```

  (`using System.Collections.Generic; using Ctxd.Battle.Sim.Net;` — file đã có.)
- [ ] **Step 4: `ShowSkillName` + DOTween pop** (`using DG.Tweening;`)

```csharp
public void ShowSkillName(string skillId, string skillName)
{
    if (_skillPopGroup == null) return;
    var pic = CtxdArt.SkillNamePic(skillId);
    if (_skillPopImage != null) { _skillPopImage.sprite = pic; _skillPopImage.enabled = pic != null; _skillPopImage.preserveAspect = true; }
    if (_skillPopText != null) _skillPopText.gameObject.SetActive(pic == null);
    if (pic == null)
    {
        if (_skillPopText == null || string.IsNullOrEmpty(skillName)) return;
        _skillPopText.text = skillName;
    }
    _skillPopTween?.Kill();
    var rt = (RectTransform)_skillPopGroup.transform;
    _skillPopGroup.alpha = 0f; rt.localScale = Vector3.one * 1.6f;
    _skillPopTween = DOTween.Sequence()
        .Append(_skillPopGroup.DOFade(1f, 0.15f))
        .Join(rt.DOScale(1f, 0.25f).SetEase(Ease.OutBack))
        .AppendInterval(1.1f)
        .Append(_skillPopGroup.DOFade(0f, 0.3f));
}
```

- [ ] **Step 5: `BuildHud()` dựng cụm mới** (toạ độ cụ thể — phe DEF đối xứng qua anchor (1,1), x đổi dấu, text align Right):
  - Khung avatar: `Frame("OffAvatar", rt, (0,1),(0,1),(0,1), (24,-24), (96,96), CtxdPalette.BtnBorder)`; con Image "Portrait" stretch inset 4, `preserveAspect` (bật ở runtime — set field). Sau Frame: `AddComponent<RectMask2D>()` vào Fill? KHÔNG cần — ảnh nền trong suốt, inset đủ.
  - Tên: giữ GO `OffName` — dời `pos (132,-30) size (250,34)` font 22.
  - HP: thay `Bar` bằng `BarLabeled("OffHp", rt, (0,1), (132+120=252? — GIỮ pos cũ (180,-64) đổi thành (252,-62)) …`— cụ thể: `BarLabeled("OffHp", rt, new Vector2(0,1), new Vector2(252,-62), new Vector2(240,20), CtxdPalette.HpAlly, out var offTroops)`.
  - Morale bar cũ BỎ (thay bằng sao) — xoá 2 dòng `Bar("OffMorale"…)/Bar("DefMorale"…)`, field `_offMorale/_defMorale` trong BattleHudUI GIỮ (null-safe sẵn) nhưng KHÔNG wire nữa; SetActiveGenerals bỏ nhánh set morale fill (thay bằng SetStars).
  - Sao nộ: 6 `Image` "OffStar0..5" 24×24 tại x=132+i*27, y=-92, anchor (0,1) — tạo bằng vòng for, sprite gán RUNTIME (SetStars) nên Forge chỉ tạo Image trống màu trắng α0.28.
  - Buff row: `NewRect("OffBuffs", rt, (0,1),(0,1),(0,1), (24,-128), (420,44))` + `HorizontalLayoutGroup` (spacing 6, childControlWidth/Height false, childAlignment UpperLeft); con template "BuffIcon" (40×40 Image trắng + con TMP "N" font 14 góc dưới-phải (anchor (1,0), pos (-2,2), size 20×16, màu vàng)) `SetActive(false)`.
  - Banner ribbon: trước khi add TMP banner — con "BannerBg" của bannerGo: Image đen α0.55 stretch; 2 con "LineT/LineB" Image `BtnBorder` cao 2 stretch-x tại top/bottom. TMP banner add SAU (vẽ trên).
  - Skill pop: `NewRect("SkillPop", rt, (0.5f,0.5f),(0.5f,0.5f),(0.5f,0.5f), (0,60), (620,150))` + CanvasGroup α0; con Image "Pic" stretch preserveAspect; con TMP "Txt" stretch font 60 màu `CtxdPalette.SkillPop`, outline giữ StyleText (đen) — fallback text.
  - Stance wheel: đổi màu 4 nút qua palette (crimson/blue/gold như Task 3) — vị trí giữ.
  - Wire mọi field mới bằng `SetField` (offPortrait, defPortrait, offTroops, defTroops, offStars, defStars, offBuffRow, defBuffRow, buffTemplate, skillPopGroup, skillPopImage, skillPopText).
- [ ] **Step 6: Compile → rebuild scene → console 0 error.**
- [ ] **Step 7: Commit** — `feat(ui-skin): đợt 2a — HUD avatar/sao nộ/số quân/buff row/banner ribbon/skill pop (forge + UI)`.

### Task 5 (Đợt 2b): Wire Director + điền bảng map buff & skill-name + verify trận thật

**Files:**
- Modify: `Assets/Ctxd/Battle/ServerBattleDirector.cs:293` (RenderFields) + case `SkillCast/TacticCast` (~dòng 421-427)
- Modify: `Assets/Ctxd/UI/CtxdSkin.cs` (điền `FxToBuff`, `SkillNameMap`, xichbich nếu chốt được)

**Interfaces:** Consumes `BattleHudUI.SyncSideEffects/ShowSkillName` (Task 4); `BattleEvent.TacticId/TacticName`.

- [ ] **Step 1: RenderFields** — sau `_hud.SetActiveGenerals(off, def);` thêm `if (_hud != null) _hud.SyncSideEffects(_state.Offense, _state.Defense);`.
- [ ] **Step 2: case SkillCast/TacticCast** — trong thân case (trước/cạnh chỗ render FX skill hiện có) thêm:

```csharp
if (_hud != null && (!string.IsNullOrEmpty(e.TacticId) || !string.IsNullOrEmpty(e.TacticName)))
    _hud.ShowSkillName(e.TacticId, e.TacticName);
```

- [ ] **Step 3: `SkillNameMap`** — KẾT LUẬN ĐIỀU TRA (đã chạy trước khi viết plan): OCR agent không tin cậy với thư pháp; tự xác minh bằng mắt được `warSkillName/5`=Phi Vũ, `6`=Mãnh Công, `7`=Đột Kích, `10`=Thương Lâm Tiễn Vũ — KHÔNG cái nào trùng 6 chiến pháp roster (wushen/roar/dragonspear/polei/volley/volleyx). → `SkillNameMap` khởi tạo RỖNG kèm comment 4 entry đã xác minh (dạng chú thích để đời sau thêm khi cần); pop luôn chạy nhánh TEXT thư pháp với `TacticName` server gửi. Xác nhận grep `grep '"Skill' Server/data/roster.json` đủ 8 tướng để chắc không bỏ sót skill trùng 4 ảnh trên; nếu tướng 7/8 có skill trùng → thêm entry đó.
- [ ] **Step 4: Điền `FxToBuff`** — `ls` + Read 8 icon `warBuff` (id 1..20 chọn 8): ghi nhận icon lửa / khiên / kiếm / lá. Điền map cho các FxId server đang phát: grep FxId thực tế: `grep -o '"fx[^"]*"' Server/data/*.json | sort -u` + `grep "FxId" Server/**/*.cs | head`. Mỗi FxId → icon hợp nghĩa (lửa→icon lửa, buff def→khiên, buff atk→kiếm); FxId lạ → default (đã có).
- [ ] **Step 5: `dotnet build` + test** — `dotnet test` trong thư mục Server tests (lệnh như harness cũ; xem `Server/README` hoặc chạy `dotnet test` ở root nếu có sln). Kỳ vọng: pass toàn bộ như baseline (114/114) — thay đổi client-only, test server không ảnh hưởng; chạy để chắc KHÔNG lỡ tay đụng file Sim share.
- [ ] **Step 6: Play-test trận thật** — MCP: play; Lobby→`XUẤT CHINH`→chọn 2-3 tướng (bấm `Card0/Card1` qua Button component)→`TIẾP TỤC`→chọn `Thao Trường FX`→`VÀO TRẬN`; đợi 4s; screenshot: PHẢI thấy avatar 2 tướng + sao nộ + số quân; bấm `Dùng skill — ATT` (TestApiPanel) → screenshot thấy skill pop (ảnh thư pháp hoặc text hồng); `KillRow` vài phát tới khi có buff/fire → screenshot buff row. Stop play. (Server side: nếu `dotnet run --project Server -- server` chưa chạy, mở bằng PowerShell `run_in_background`.)
- [ ] **Step 7: Commit** — `feat(ui-skin): đợt 2b — wire Director (buff row + skill pop), bảng map buff/skill-name từ OCR`.

### Task 6 (Đợt 3): 4 màn ngoài trận

**Files:**
- Modify: `Assets/Ctxd/Editor/UIForge.cs` (`BuildLobby/BuildSelectGeneral/BuildGeneralCard/BuildLineupSlot/BuildSelectStage/BuildStageCard/BuildResult`)
- Modify: `Assets/Ctxd/UI/SelectGeneralUI.cs` (Card thêm `public Image portrait;` + nạp trong OnBeforeShowAsync)
- Modify: `Assets/Ctxd/UI/SelectStageUI.cs` (StageCard thêm `public Image thumb;` + nạp)
- Modify: `Assets/Ctxd/UI/ResultUI.cs` (đổi màu chữ theo nền giấy — title đỏ đô khi thua giữ, outcome/reward màu `InkOnPaper`)

**Interfaces:** Consumes `CtxdArt.Portrait/StageBg`, `SkinIndexForge.FindSprite` (bake-time), `Frame`.

- [ ] **Step 1: Lobby** — sau `FullBg`: Image "Hero" `NewRect("Hero", rt, (0.5f,1),(0.5f,1),(0.5f,1),(0,-40),(900,250))` sprite = `SkinIndexForge.FindSprite("eff/Recruit", "1")`, preserveAspect, raycastTarget false; Title dời `(0,-120)`, Sub `(0,-180)`. Header panel → `Frame` + con Image "Face" 120×120 trái sprite `FindSprite("tacticalGeneralPicMax", "liubei")` preserveAspect (trang trí, texts dời offMin.x 150). Nút giữ tên/vị trí.
- [ ] **Step 2: SelectGeneral card** — trong `BuildGeneralCard`: nền → `Frame`; con Image "Portrait" `NewRect("Portrait", cardRt, (0,0.5f),(0,0.5f),(0,0.5f),(60,0),(96,108))` preserveAspect raycastTarget false; texts dời offMin.x từ 16 → 128 (name/troop), level/power giữ đáy. Struct `Card` thêm field + Forge gán `portrait = img`. `SelectGeneralUI.OnBeforeShowAsync` thêm:

```csharp
if (_cards[i].portrait != null)
{
    var sp = CtxdArt.Portrait(g.Id);
    _cards[i].portrait.sprite = sp;
    _cards[i].portrait.enabled = sp != null;
}
```

  Slot: nền `Frame`; huy hiệu số: `Frame("Badge", rt, (0,0.5f),(0,0.5f),(0,0.5f),(30,0),(34,34), CtxdPalette.BtnBorder, CtxdPalette.BtnGold)` + TMP số `i+1` giữa; label dời offMin.x 56.
- [ ] **Step 3: SelectStage card** — nền → `Frame`; con Image "Thumb" `(0,0.5f) pos (98,0) size (168,92)` + con overlay Image đen α0.45 stretch + texts dời offMin.x 16→196. Struct thêm `thumb`; runtime nạp `CtxdArt.StageBg(s.Id, s.Terrain)`. **Chốt xichbich:** Read `warBG/2,3,4,7` JPG — ảnh nào có sông/nước → thêm `["stage_xichbich"] = "warBG/<n>"` + sửa nhánh `"Water"` cùng key trong `CtxdSkin.StageBg`; không có → dùng `"warBG/100"`.
- [ ] **Step 4: Result cuộn giấy** — panel: bỏ `Panel(...)` màu; thay: `var panel = NewRect("Panel", rt, …(0.5f,0.5f), Vector2.zero, new Vector2(900, 527));` + Image sprite `FindSprite("windowBG", "1")`, `type = Image.Type.Simple`, preserveAspect. Title tại `(0,-70)` từ đỉnh panel (offMin/offMax như builder cũ chỉnh theo 900×527: title offMin (120,380) offMax (-120,-60); outcome (140,255)/(-140,-210); reward (150,130)/(-150,-320)). `ResultUI.OnBeforeShowAsync`: `_outcome.color = CtxdPalette.InkOnPaper; _reward.color = CtxdPalette.InkOnPaper;` title thắng `#B8860B` đậm / thua `#8B2F24`. Nút VỀ SẢNH `(0, -30)` DƯỚI panel (anchor (0.5f,0), pos (0,50) của root — giữ chỗ cũ).
- [ ] **Step 5: Compile → rebuild → play-test 4 màn** (Lobby hero + face; SelectGeneral 8 thẻ có chân dung; SelectStage thumb; Result cuộn giấy — ép thắng nhanh: play tới trận rồi spam `Kill hàng 1 — DEF` tới Finished) — screenshot từng màn, Read đối chiếu.
- [ ] **Step 6: Commit** — `feat(ui-skin): đợt 3 — chân dung thẻ tướng, thumb màn, Lobby hero, Result cuộn giấy`.

### Task 7 (Đợt 4): Polish — fade-in màn, GIÁC sáng, TestApiPanel gọn

**Files:**
- Modify: `Assets/Ctxd/UI/CtxdSkin.cs` (+`CtxdUiFx.Enter`)
- Modify: `Assets/Ctxd/UI/SelectGeneralUI.cs`, `SelectStageUI.cs`, `ResultUI.cs`, `LobbyUI.cs` (gọi Enter trong OnBeforeShowAsync)
- Modify: `Assets/Ctxd/UI/BattleHudUI.cs` (GIÁC màu + sao cuối blink)
- Modify: `Assets/Ctxd/Editor/UIForge.cs` (`BuildPanel` gọn + CanvasGroup cho 4 màn)

**Interfaces:** Produces `CtxdUiFx.Enter(CanvasGroup cg, RectTransform pop = null)`.

- [ ] **Step 1: `CtxdUiFx`** (thêm cuối `CtxdSkin.cs`, `using DG.Tweening;`):

```csharp
public static class CtxdUiFx
{
    /// <summary>Fade-in màn 0.25s (+ scale-pop panel 0.94→1 nếu truyền).</summary>
    public static void Enter(CanvasGroup cg, RectTransform pop = null)
    {
        if (cg != null) { cg.alpha = 0f; cg.DOKill(); cg.DOFade(1f, 0.25f); }
        if (pop != null) { pop.DOKill(); pop.localScale = Vector3.one * 0.94f; pop.DOScale(1f, 0.25f).SetEase(Ease.OutCubic); }
    }
}
```

- [ ] **Step 2: Forge** — `UIRoot` các màn Screen (Lineup/Lobby/SelGen/SelStage/Result): `root.AddComponent<CanvasGroup>()`; wire vào field mới `[SerializeField] private CanvasGroup _screenGroup;` của 4 UI class (Result thêm cả `_panelRt`). Mỗi `OnBeforeShowAsync` (cuối hàm): `CtxdUiFx.Enter(_screenGroup)` (Result: `CtxdUiFx.Enter(_screenGroup, _panelRt)`).
- [ ] **Step 3: GIÁC + sao blink** — `SetActiveGenerals`: đã có `_giac.interactable = off.CanCast`; thêm: khi CanCast chuyển false→true (`bool _giacWasOn` field): `((RectTransform)_giac.transform).DOPunchScale(Vector3.one * 0.12f, 0.35f, 6, 0.7f);` và sao cuối `_offStars[5].DOKill(); _offStars[5].DOFade(0.5f, 0.5f).SetLoops(-1, LoopType.Yoyo)` khi CanCast, `DOKill()+color trắng` khi tắt.
- [ ] **Step 4: TestApiPanel gọn** — `BuildPanel`: size `(300, 470)`, pos `(-12, -240)`; bg → `Frame(border: PanelBorder, fill: đen α0.62)`; title font 18 y=-24 size (290,32); nút `(280,36)` font 15, bước y 40, bắt đầu -56; màu ATT `BtnBlue`, DEF `BtnCrimson`.
- [ ] **Step 5: Compile → rebuild → play-test flow đủ** (fade thấy được, GIÁC punch khi đầy nộ — dùng `Dùng skill` buttons/đợi AutoStance) → screenshot HUD cuối.
- [ ] **Step 6: Commit** — `feat(ui-skin): đợt 4 — fade-in màn, GIÁC/sao nộ sống động, TEST panel gọn`.

### Task 8: Verify tổng E2E + wiki + memory

- [ ] **Step 1:** `read_console` clear → play → đi trọn flow Lobby→SelectGeneral→SelectStage→Battle (đợi ≥2 hiệp + 1 skill)→ép kết thúc→Result→VỀ SẢNH→Lobby. Console: 0 Error/Exception mới trong toàn phiên.
- [ ] **Step 2:** Screenshot 5 màn cuối cùng lưu `Assets/Screenshots/reskin-<màn>.png`, Read đối chiếu với 2 ảnh mẫu của chủ dự án — checklist: khung avatar+chân dung ✓, sao nộ ✓, buff icon ✓, banner ribbon ✓, skill pop thư pháp ✓, thẻ tướng có chân dung ✓, thumb màn ✓, cuộn giấy ✓.
- [ ] **Step 3:** `dotnet test` lần cuối (như Task 5 Step 5).
- [ ] **Step 4:** Wiki log `.wiki/wiki/technical/ui-reskin-2026-08-05.md` (theo pattern log đợt trước: làm gì, file nào, verify gì, còn mở gì) + commit `docs(wiki): log UI reskin`.
- [ ] **Step 5:** Cập nhật memory `ctxd-battle-fx-branch.md` (nhánh thêm UI reskin; các bảng map nằm ở CtxdSkin; index forge menu mới).

## Self-Review (đã chạy)

1. **Spec coverage:** Đợt 1→Task 3; Đợt 2→Task 4+5; Đợt 3→Task 6; Đợt 4→Task 7; index/loader→Task 1+2; verify→mỗi task + Task 8. Spec mục "sao nhấp nháy khi CanCast" (Đợt 4) → Task 7 Step 3 ✓; "TestApiPanel gọn" ✓; "banner ribbon" → Task 4 Step 5 ✓.
2. **Placeholder scan:** 2 điểm hoãn CÓ TIÊU CHÍ + fallback rõ (xichbich chọn từ 4 ảnh cụ thể; SkillNameMap/FxToBuff điền theo thủ tục OCR/xem-icon với quy tắc match) — không phải TBD mù.
3. **Type consistency:** `SyncSideEffects(SideSnapshot, SideSnapshot)` thống nhất Task 4/5; `CtxdArt.*` chữ ký thống nhất Task 2/4/6; `Frame/BarLabeled` thống nhất Task 3/4/6/7; field `_screenGroup/_panelRt` Task 7 tự khai báo ✓.
